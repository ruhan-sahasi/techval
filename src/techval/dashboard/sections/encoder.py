"""Peer encoder. A stub until this section's collector is built."""

from __future__ import annotations

ID = "encoder"
TITLE = "Peer encoder"
INPUTS: list[str] = []


def collect(ctx) -> dict:
    return {"status": "not_built"}
