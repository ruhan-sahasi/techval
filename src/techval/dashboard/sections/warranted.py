"""Warranted multiple. A stub until this section's collector is built."""

from __future__ import annotations

ID = "warranted"
TITLE = "Warranted multiple"
INPUTS: list[str] = []


def collect(ctx) -> dict:
    return {"status": "not_built"}
